#pragma once
#include <vector>

double Average(const std::vector<int>);
double Median(const std::vector<int>);
double QuadraticMean(const std::vector<int>);
double Variance(const std::vector<int>);
#define WIN32_LEAN_AND_MEAN             // Исключите редко используемые компоненты из заголовков Windows
